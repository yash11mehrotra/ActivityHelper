<h1><%= (@user.email).split("@")[0] %></h1>
<h1>
 <%= gravatar_for @user %>
</h1>


<%= button_to "Send me message", new_user_messages_path(@user), :method => "get",class: "btn btn-primary" if @user != current_user%>
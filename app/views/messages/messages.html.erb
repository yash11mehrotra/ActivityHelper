<div class="container">
	<ul>

		<% @msg.each do |a| %>
		<%if (a.sender_id==current_user.id)|| (a.receiver==current_user)%>
		<li><%= a.body %></li>
		<li><%= current_user %></li>
		<% end %>
		<%end%>
	</ul>	
</div>
